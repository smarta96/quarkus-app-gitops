apiVersion: v1
kind: Service
metadata:
  name: quarkus-service
  namespace: cicd-project
spec:
  selector:
    app: quarkus-deployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
